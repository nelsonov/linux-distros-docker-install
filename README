Scripts to set up docker on various Linux distributions

Currently supported:
	  Debian Jessie

You will need to have git already installed.

git clone https://github.com/nelsonov/linux-distros-docker-install.git